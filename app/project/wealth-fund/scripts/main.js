AOS.init({
  duration: 700,
  once: true,
});


var html_typed = new Typed('#html-typed', {
  stringsElement: '#html-typed-strings',
  typespeed: 60,
  backspeed: 60,
  loop:false  
});

var css_typed = new Typed('#css-typed', {
  stringsElement: '#css-typed-strings',
  typespeed: 60,
  backspeed: 60,
  loop:false  
});

var js_typed = new Typed('#js-typed', {
  stringsElement: '#js-typed-strings',
  typespeed: 60,
  backspeed: 60,
  loop:false  
});

  